<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="githubtest.qianfeng.com.ecookrooms" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="activity.StartActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Web_Activity"
            android:label="@string/title_activity_web_" />
        <activity android:name="activity.About_US" />
        <activity
            android:name=".Login_Activity"
            android:label="@string/title_activity_login_" />
        <activity android:name="activity.Shake2" />
        <activity android:name="activity.Caipu_particular" />
        <activity
            android:name=".Collection"
            android:label="@string/title_activity_collection" >
        </activity>
        <activity android:name=".Find_Friends"></activity>
    </application>

</manifest>
